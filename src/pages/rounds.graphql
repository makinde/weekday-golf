query defaultCourseRoundsPage($courseId: Int!) {
  course(id: $courseId) {
    id
    slug
    name
    img
  }

  roundsForChart: playerRounds(
    where: {
      courseId: { _eq: $courseId },
      complete: { _eq: true },
    }
  ) {
    ...playerRoundForChart
  }
}