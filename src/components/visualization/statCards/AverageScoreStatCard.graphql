query averageScoreStatCard(
  $courseId: Int,
  $playerId: Int,
) {
  playerRoundStats(where: {
    courseId: { _eq: $courseId },
    playerId: { _eq: $playerId },
  }) {
    aggregate {
      avg {
        relativeScore
      }
    }
  }
}